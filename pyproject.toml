[project]
name = "learn_nn"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = ">=3.9"
dependencies = [
    "datasets>=4.5.0",
    "numpy>=2.0.2",
    "torch>=2.8.0",
    "tqdm>=4.70.0",
]

[project.scripts]
seq2seq = "models.seq2seq:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
